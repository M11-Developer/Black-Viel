#!/bin/bash
# BLACK VEIL — macOS Double-Click Launcher
# Just double-click this file in Finder

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"
chmod +x Black-Veil.sh
./Black-Veil.sh
